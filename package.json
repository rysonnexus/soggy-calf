{
  "name": "soggy-calf",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run db:up:optional && concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "db:up:optional": "npm run db:up || echo [warn] Docker postgres not started; continuing with existing DB setup.",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose stop postgres",
    "db:logs": "docker compose logs -f postgres",
    "build": "npm run build --workspace=frontend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
